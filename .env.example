# Docker Compose 环境变量配置
# 复制此文件为 .env 并修改相应值

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456

# Redis
REDIS_PASSWORD=123456

# Backend（本地直连）
BACKEND_PORT=3001

# JWT Secrets - 生产环境必须修改！(不能包含 "your_" 前缀，否则后端拒绝启动)
JWT_SECRET=change_me_to_a_random_string_at_least_32_chars!!
JWT_ADMIN_SECRET=change_me_to_another_random_string_at_least_32!!

# 默认管理员密码 - 仅首次初始化时使用
DEFAULT_ADMIN_PASSWORD=change_me_to_strong_password

# 上游配置
GONGXI_BASE_URL=http://host.docker.internal:3000
GONGXI_API_KEY=sk_your_gongxi_api_key
